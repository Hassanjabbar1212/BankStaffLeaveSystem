@model Employees
<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Extended Form</title>
    <!-- Bootstrap CSS link (make sure to include the correct version) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" integrity="sha512-4o5Y8Foa6jFIepL6OsFJ6oSJwdGCN5Wv2T3LpF2FZl9BqSsO3CxNjkD5i+M/FfVqvb5+7eI+cEL8eNtEFsM6wg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f0f0f0; /* Grey background color */
            align-items: center;
            justify-content: center;
        }

        form {
            background-color: rgba(255, 255, 255, 0.8); /* Semi-transparent white background for better readability */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Optional: Add a subtle box shadow for depth */
        }
    </style>
</head>
<body>
    <form asp-controller="Home" asp-action="Register" method="post">
        <div class="mb-3">
            <label asp-for="Email" for="exampleInputEmail1" class="form-label">Email address</label>
            <input asp-for="Email" type="email" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" required>
            <div id="emailHelp" class="form-text">We'll never share your email with anyone else.</div>
        </div>
        <div class="mb-3">
            <label asp-for="FName" for="firstName" class="form-label">First Name</label>
            <input asp-for="FName" type="text" class="form-control" id="firstName" required>
        </div>
        <div class="mb-3">
            <label asp-for="LName" for="lastName" class="form-label">Last Name</label>
            <input asp-for="LName" type="text" class="form-control" id="lastName" required>
        </div>
        <div class="mb-3">
            <label asp-for="Password" for="password" class="form-label">Password</label>
            <input asp-for="Password" type="text" class="form-control" id="password" required>
        </div>
        <div class="mb-3">
            <label asp-for="Address" for="address" class="form-label">Address</label>
            <input asp-for="Address" type="text" class="form-control" id="address" required>
        </div>
        <input id="imageFile" type="file" name="imageFile" onchange="previewImage(event)" />
        <div id="imagePreviewContainer">
            <img id="imagePreview" src="#" alt="Preview" style="display: none; max-width: 300px; max-height: 300px;" />
        </div>
        <button type="submit" id="submitBtn">Submit</button>
    </form>
</body>
</html>





